# Release

`@molt` v1 is declared as version `1.0.0` in the root [`VERSION`](../VERSION) file.

This workspace currently contains the completed implementation, final docs, verified examples, and a passing quality gate.

If you are working from a Git-enabled checkout, the intended release tag is:

```txt
v1.0.0
```

The current workspace does not include Git repository metadata, so the tag itself cannot be created here.
