import collections from "std:collections"
import io from "std:io"

total = 0
for item in [1, 2, 3] -> {
  total = total + item
}

io.print(total)

chars = []
for ch in "ok" -> {
  collections.push(chars, ch)
}

io.print(chars)
