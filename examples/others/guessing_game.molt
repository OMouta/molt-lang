# Guess the fixed secret number. Keep entering guesses until you win.

secret = 7
won = false

while not won -> {
  print("guess a number between 1 and 10")
  guess = to_number(input())

  if guess == secret -> {
    print("you got it!")
    won = true
  } else -> if guess < secret -> {
    print("too low")
  } else -> {
    print("too high")
  }
}
