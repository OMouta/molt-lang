import "std:io"
import "std:meta"

x = 2
outer = 10

fragment = @{ x + outer }
maker = @{ fn(x) = ~(fragment) }
f = meta.eval(maker)
io.print(f(5))

wrap = ~{ $body -> fn(x) = $body }
g = meta.eval(@{ x + outer } ~ wrap)
io.print(g(7))
