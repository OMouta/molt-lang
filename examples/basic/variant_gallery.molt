import "std:collections" as c
import "std:io"
import "std:meta"

fn run(code, mutation) = meta.eval(code ~ mutation)

xs = []
c.push(xs, run(@{ 2 + 3 }, ~{ + -> * }))
c.push(xs, run(@{ c.len("molt") + 1 }, ~{ 1 -> 3 }))
c.push(xs, meta.type(@{ 0 }))

io.print(xs)
