import "std:io"

describe = fn(value) = match value {
  1 -> "one"
  2 -> "two"
  name -> name
}
fallback = match false {
  true -> "yes"
  _ -> "no"
}
io.print(describe(2))
io.print(
  describe("molt"),
)
io.print(fallback)
